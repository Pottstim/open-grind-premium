# Boosting, WIP

> [!NOTE]
> This page is a work in progress. Endpoints below haven't been fully researched.

## Get boost sessions, WIP

Response type: `BoostSessionResponse` (undocumented).

```
GET /v2/boost/sessions
```

## Redeem boost, WIP

Response type: `BoostRedeemedResponse` (undocumented).

```
POST /v1/consumables/redeem/boost
```

## Set mega boost preferences, WIP

Body type: `SuperBoostPreferencesRequest` (undocumented).

```
POST /v1/boost/preferences/mega
```

## Set super boost preferences, WIP

Body type: `SuperBoostPreferencesRequest` (undocumented).

```
POST /v1/boost/preferences/super
```

## Set standard boost preferences, WIP

Body type: `BoostPreferencesRequest` (undocumented).

```
POST /v1/boost/preferences/standard
```

## Get consumables inventory, WIP

Response type: `ConsumableInventoryResponse` (undocumented).

```
GET /v1/consumables/inventory
```
