# Heatmap, WIP

> [!NOTE]
> This page is a work in progress. Endpoints below haven't been fully researched.

## Get all explore heatmaps, WIP

Response type: `ExploreHeatmapsResponse` (undocumented).

```
GET /v1/explore_heatmaps/all_locations
```

## Query explore heatmaps, WIP

Body type: `ExploreHeatmapsRequest` (undocumented).

Response type: `ExploreHeatmapsResponse` (undocumented).

```
POST /v1/explore_heatmaps
```
