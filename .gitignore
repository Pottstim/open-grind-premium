.DS_Store
node_modules
/build
/.svelte-kit

# SvelteKit production build output (regenerated by `bun run build`)
/dist
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Nix
/result
/result-*

# direnv — developer-local; may leak machine-specific paths
.envrc
.direnv/

# Android signing
keystore.properties
*.jks
*.keystore

# Build outputs
build-output/

# Rust
/target/
src-tauri/target/

.local

# Tauri generated artifacts (regenerated by `tauri android init` / `tauri build`)
# CI removes and regenerates src-tauri/gen/android on every build; do not commit.
/src-tauri/gen/